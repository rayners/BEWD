doctype html
html(lang="en", ng-app="bewd.tictactoe.board")
    head
      include ./includes/bower_bootstrap.jade
      title Game ##{id}
    body  
      .container
        h1 Game ##{board.id}
        tic-tac-toe-board(the-board="#{JSON.stringify(board.board)}")
        //get it out of jade and into angular so we can work with it
